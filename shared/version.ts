/** rexarr version shown in the UI, logs, System → Status and the MusicBrainz user agent (package.json keeps 0.1.4: npm needs three parts). */
export const APP_VERSION = '0.1.4.2';

/** Source, documentation, releases and issues. */
export const REPO_URL = 'https://github.com/MoonlightLaboratory/rexarr';
