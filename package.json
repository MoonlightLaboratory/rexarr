{
  "name": "rexarr",
  "version": "0.1.5",
  "private": true,
  "license": "GPL-3.0-or-later",
  "description": "Remux-first transcoding companion for the *arr stack (beta): finds Blu-ray remuxes via Radarr/Sonarr/Lidarr/Prowlarr and re-encodes them with FFmpeg or fre:ac using your own profiles.",
  "homepage": "https://github.com/MoonlightLaboratory/rexarr#readme",
  "repository": { "type": "git", "url": "git+https://github.com/MoonlightLaboratory/rexarr.git" },
  "bugs": { "url": "https://github.com/MoonlightLaboratory/rexarr/issues" },
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "node server/dist/server/src/index.js",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client",
    "test": "npm test -w server",
    "test-media": "node scripts/make-test-media.mjs test-media"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22.12"
  }
}
